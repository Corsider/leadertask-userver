version: "3.9"

services:
  userver:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jwt_auth_service
    ports:
      - "8080:8080"
    restart: unless-stopped
    cap_add:
      - SYS_PTRACE
    security_opt:
      - seccomp=unconfined
    user: "1000:1000"